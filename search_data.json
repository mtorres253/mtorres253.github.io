---
layout: null
---
{
  {% for page in site.pages %}

    "{{ page.url | slugify }}": {
      "title": "{{ page.title | xml_escape }}",
      "url": " {{ page.url | xml_escape }}",
      "body": " {{ page.body | xml_escape }}"
    }
    {% unless forloop.last %},{% endunless %}
  {% endfor %}
}